<h1>Listing pictures</h1>
<script> $(function(){alert('lol');});</script>
<table>
  <tr>
    <th>Image</th>
    <th>Geoloc lat</th>
    <th>Geoloc long</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pictures.each do |picture| %>
  <tr>
    <td><div class='cont_small'><%= image_tag picture.image, :class => 'image'%></div>
      <p><%= "Posted by #{picture.user.email}" %><p></td>
    <td><%= picture.geoloc_lat %></td>
    <td><%= picture.geoloc_long %></td>
    <td><%= link_to 'Show', picture %></td>
    <td><%= link_to 'Edit', edit_picture_path(picture) %></td>
    <td><%= link_to 'Destroy', picture, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Picture', new_picture_path %>
